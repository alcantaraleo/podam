general:
  build_dir: api
  
  
test:
  post:
    - echo $CIRCLE_TEST_REPORTS
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/failsafe-reports/*.xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \; -exec cp {} $CIRCLE_ARTIFACTS \;

